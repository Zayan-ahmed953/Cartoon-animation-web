version: '3.8'

services:
  cartoon-animation-web:
    build: .
    ports:
      - "3000:80"
    container_name: cartoon-animation-web
    restart: unless-stopped
    # Optional: Add environment variables if needed
    # environment:
    #   - NODE_ENV=production 